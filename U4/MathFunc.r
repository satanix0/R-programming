x <- -12.34
print(abs(x))
# Compute the absolute value of a number
abs_val <- abs(-5)
print(abs_val)

# Compute the square root of a number
sqrt_val <- sqrt(25)
print(sqrt_val)

# Compute the natural logarithm of a number
log_val <- log(10)
print(log_val)

# Compute the log10 value of a number
print(log10(100))

# Compute the exponential function of a number
exp_val <- exp(2)
print(exp_val)

# Round a number to a specified number of decimal places
round_val <- round(3.14159, digits = 2)
print(round_val)

# Trunctates the digits after the decimal point
x <- trunc(42.23685)
print(x)

# Round a number down to the nearest integer
floor_val <- floor(3.9)
print(floor_val)

# Round a number up to the nearest integer
ceiling_val <- ceiling(3.1)
print(ceiling_val)

# Find the maximum value in a set of numbers
max_val <- max(1, 2, 3, 4, 5)
print(max_val)

# Find the minimum value in a set of numbers
min_val <- min(1, 2, 3, 4, 5)
print(min_val)

# Add up all the values in a set of numbers
sum_val <- sum(1, 2, 3, 4, 5)
print(sum_val)

# Compute the arithmetic mean of a set of numbers
mean_val <- mean(1, 2, 3, 4, 5)
print(mean_val)

# Find the median value in a set of numbers
median_val <- median(1, 2, 3, 4, 5)
print(median_val)

# Compute the standard deviation of a set of numbers
sd_val <- sd(1, 2, 3, 4, 5)
print(sd_val)

# Compute the variance of a set of numbers
var_val <- var(1, 2, 3, 4, 5)
print(var_val)

# Returns the value of pi
pi_val <- pi
print(pi_val)

# Compute the sine of an angle in radians
sin_val <- sin(pi / 2)
print(sin_val)

# Compute the cosine of an angle in radians
cos_val <- cos(pi / 2)
print(cos_val)

# Compute the tangent of an angle in radians
tan_val <- tan(pi / 4)
print(tan_val)

# Compute the inverse cosine of a number in radians
acos_val <- acos(0)
print(acos_val)

# Compute the inverse sine of a number in radians
asin_val <- asin(1)
print(asin_val)

# Compute the inverse tangent of a number in radians
atan_val <- atan(1)
print(atan_val)
